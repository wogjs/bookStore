<div class="d-flex flex-column flex-md-row align-items-center justify-content-between p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
    <h5 class="my-0  font-weight-normal" onclick="location.href='/'">서점</h5>
    <div class="d-flex flex-row">
        <div class="d-flex flex-row">
            <select class="custom-select md-2" id="searchWhat" style="width:50%">
                <option selected value="no">검색방법</option>
                <option value="bookName">제목</option>
                <option value="bookAuthor">저자</option>
            </select>
            <input type="text" class="form-control" id="searchKey" placeholder="">
        </div>
        <div>
            <button class="btn btn-outline-success" id="btn-search">검색</button>
        </div>
    </div>
    <nav class="my-2 my-md-0 mr-md-3 d-flex flex-nowrap">
        <a class="p-2 text-dark" href="/basket">장바구니</a>
        {{#userid}}
            <a class="p-2 text-dark" href="/users/mypage">마이페이지</a>
        {{/userid}}
        <div class="ml-3">
            {{^userid}}
                <a class="btn btn-outline-primary" href="/users/login" role="button">로그인</a>
            {{/userid}}
            {{#userid}}
                <form action="/users/logout" method="post">
                    <button class="btn btn-outline-danger" type="submit">로그아웃</button>
                </form>
            {{/userid}}
        </div>
    </nav>

</div>